{% extends "layout.twig" %}

{% block content %}

    <div class="container d-flex align-items-center justify-content-center text-center">
        <div class="w-50">
            <h1 class="display-5 fw-bold">Login</h1>
            <form action="/login" method="post">
                <input name="input_name" class="form-control" value="" placeholder="Enter your name"/>
                <button class="btn btn-primary w-100 mt-2 mb-2">Go..</button>
            <form>
            <a href="/">Home</a>
        </div>
    </div>
    
{% endblock %}